import { dispatch, dispatchAsync } from 'appDispatcher';
import LayoutConstants from 'layoutConstants';
import LayoutService from 'layoutService';


export default {
  receiveLayout: () => {
    // let promise = LayoutService.receiveLayout(cashRegisterId);
    //
    // dispatchAsync(promise, {
    //   request: LayoutConstants.MAIN_RECEIVE_DATA,
    //   success: LayoutConstants.MAIN_RECEIVE_DATA_SUCCESS,
    //   failure: LayoutConstants.MAIN_ERROR
    // }, { });

  }
}
